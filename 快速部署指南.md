# 🚀 Firestore 規則快速部署指南

## ⚡ 方法一：Firebase 控制台（最簡單，推薦）

### 步驟：

1. **打開 Firebase 控制台**
   - 訪問：https://console.firebase.google.com/project/strengthwise-91f02/firestore/rules
   - 或訪問 https://console.firebase.google.com/ → 選擇項目 `strengthwise-91f02` → Firestore Database → 規則

2. **複製規則內容**
   - 打開項目中的 `firestore.rules` 文件
   - 複製所有內容（Ctrl+A, Ctrl+C）

3. **貼上並發布**
   - 在 Firebase 控制台的規則編輯器中
   - 刪除現有規則（或直接全選替換）
   - 貼上規則內容（Ctrl+V）
   - 點擊「發布」按鈕

4. **完成！** ✅
   - 規則會立即生效
   - 現在可以重新運行應用程序

---

## 🔧 方法二：使用命令行（需要登入）

### 步驟：

1. **打開終端機（PowerShell 或 CMD）**
   - 在項目目錄 `E:\FlutterAP\strengthwise`

2. **運行部署腳本**
   ```powershell
   powershell -ExecutionPolicy Bypass -File deploy.ps1
   ```
   
   或在終端機手動執行：
   ```bash
   # 步驟 1：登入（會在瀏覽器打開授權頁面）
   firebase login
   
   # 步驟 2：部署規則
   firebase deploy --only firestore:rules
   ```

3. **完成！** ✅

---

## 📋 規則內容預覽

規則文件位置：`firestore.rules`

主要規則：
- ✅ `exerciseTypes` - 公開讀取
- ✅ `bodyParts` - 公開讀取  
- ✅ `exercises` - 公開讀取
- 🔒 `users` - 需要認證，只能訪問自己的數據
- 🔒 `workoutPlans` - 需要認證
- 🔒 `bookings` - 需要認證

---

## ✅ 驗證部署

部署完成後：
1. 重新運行 Flutter 應用
2. 檢查是否還有 `permission-denied` 錯誤
3. 如果沒有錯誤，表示部署成功！

---

## 🆘 遇到問題？

### 問題：仍然看到 permission-denied
- 確保規則已發布（檢查 Firebase 控制台）
- 清除應用緩存：`flutter clean` 然後重新運行

### 問題：無法登入 Firebase CLI
- 使用**方法一**（Firebase 控制台）更簡單
- 確保有項目的編輯權限

### 問題：找不到項目
- 檢查項目 ID：`strengthwise-91f02`
- 確認 `.firebaserc` 文件中的項目 ID 正確

