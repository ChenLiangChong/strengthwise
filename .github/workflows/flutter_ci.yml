# StrengthWise Flutter CI/CD
# 觸發條件：PR 到 main、push 到 main、手動觸發

name: Flutter CI

on:
  push:
    branches: [master, main, develop]
  pull_request:
    branches: [master, main]
  workflow_dispatch: # 手動觸發

env:
  JAVA_VERSION: "17"

jobs:
  # ============================================
  # Job 1: 分析與測試
  # ============================================
  analyze_and_test:
    name: 分析與測試
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: ${{ env.JAVA_VERSION }}
          cache: 'gradle'  # 快取 Gradle 依賴，避免 Maven 403

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          cache: true

      - name: 📦 Get Dependencies
        run: flutter pub get

      - name: 📝 Create .env file
        run: |
          touch .env
          echo "# CI environment - placeholder values" >> .env

      - name: 🔍 Analyze Code
        run: flutter analyze --no-fatal-infos --no-fatal-warnings
        continue-on-error: false

      - name: 🧪 Run Tests
        run: flutter test --coverage
        continue-on-error: false

      - name: 📊 Upload Coverage
        uses: codecov/codecov-action@v4
        with:
          file: coverage/lcov.info
          fail_ci_if_error: false
        continue-on-error: true

  # ============================================
  # Job 2: 建置 Android（僅 main 分支）
  # ============================================
  build_android:
    name: 建置 Android
    runs-on: ubuntu-latest
    needs: analyze_and_test
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: ${{ env.JAVA_VERSION }}
          cache: 'gradle'  # 快取 Gradle 依賴，避免 Maven 403

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          cache: true

      - name: 📦 Get Dependencies
        run: flutter pub get

      - name: 📝 Create .env file
        run: |
          touch .env
          echo "# CI environment - placeholder values" >> .env

      # 解碼 google-services.json（Firebase 配置）
      - name: 🔥 Create google-services.json
        env:
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: |
          printf '%s' "$GOOGLE_SERVICES_JSON" | base64 -d > android/app/google-services.json

      # 解碼 Keystore（從 GitHub Secrets）
      - name: 🔑 Decode Keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          echo "$KEYSTORE_BASE64" | base64 --decode > android/app/strengthwise-release.keystore

      # 建立 key.properties
      - name: 🔐 Create key.properties
        env:
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        run: |
          cat > android/key.properties << EOF
          storePassword=$STORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          keyAlias=$KEY_ALIAS
          storeFile=strengthwise-release.keystore
          EOF

      - name: 🏗️ Build APK
        run: flutter build apk --release

      - name: 📦 Build AAB
        run: flutter build appbundle --release

      - name: 📤 Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: build/app/outputs/flutter-apk/app-release.apk

      - name: 📤 Upload AAB Artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-aab
          path: build/app/outputs/bundle/release/app-release.aab

  # ============================================
  # Job 3: 建置 iOS（可選，需要 macOS runner）
  # ============================================
  # build_ios:
  #   name: 建置 iOS
  #   runs-on: macos-latest
  #   needs: analyze_and_test
  #   if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
  #
  #   steps:
  #     - name: 📥 Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: 🐦 Setup Flutter
  #       uses: subosito/flutter-action@v2
  #       with:
  #         flutter-version: ${{ env.FLUTTER_VERSION }}
  #         channel: "stable"
  #         cache: true
  #
  #     - name: 📦 Get Dependencies
  #       run: flutter pub get
  #
  #     - name: 🏗️ Build iOS (no codesign)
  #       run: flutter build ios --release --no-codesign
