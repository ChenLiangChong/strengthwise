---
description: "時間處理統一規範：DateTimeUtils 工具類使用指南。適用於所有涉及時間的代碼。"
globs: "**/*.dart"
alwaysApply: false
---

# 時間處理規範（v2.2 架構）

<critical>
所有時間操作必須使用 `DateTimeUtils` 工具類（`lib/utils/datetime_utils.dart`）
</critical>

## ✅ 必須遵守

### 1. 時間解析

```dart
import 'package:strengthwise/utils/datetime_utils.dart';

// ✅ 正確
final dt = DateTimeUtils.parseIsoTimestamp(json['created_at']);

// ❌ 禁止直接使用
final dt = DateTime.parse(json['created_at']);
```

### 2. 時間格式化

```dart
// ✅ 正確
'created_at': DateTimeUtils.formatToUtcIso(DateTime.now())

// ❌ 禁止直接使用
'created_at': DateTime.now().toUtc().toIso8601String()
```

### 3. PostgreSQL 時間戳

```dart
// ✅ 正確（PostgreSQL 格式：2025-12-15 09:00:00+00）
final dt = DateTimeUtils.parsePostgresTimestamp(timestamp);

// ❌ DateTime.parse 無法解析此格式
final dt = DateTime.parse(timestamp); // FormatException!
```

### 4. TSTZRANGE 處理

```dart
// 解析
final range = DateTimeUtils.parseTstzRange(json['time_range']);
final startTime = range['start']!;
final endTime = range['end']!;

// 格式化
'time_range': DateTimeUtils.formatToTstzRange(startTime, endTime)
```

## 🏗️ 架構原則

```
┌────────────────────────────────────────────┐
│  UI 層                                      │
│  DateTime（本地時間）                        │
│  ✅ 直接顯示：Text('${model.createdAt.hour}')│
│  ❌ 不需要 .toLocal() 轉換                   │
└──────────────┬─────────────────────────────┘
               │
┌──────────────▼─────────────────────────────┐
│  Model 層（fromSupabase）                   │
│  DateTimeUtils.parseIsoTimestamp()         │
│  → 返回本地時間                              │
└──────────────┬─────────────────────────────┘
               │
┌──────────────▼─────────────────────────────┐
│  Service 層（toSupabase / 查詢）            │
│  DateTimeUtils.formatToUtcIso()            │
│  → 轉為 UTC 儲存                            │
└──────────────┬─────────────────────────────┘
               │
┌──────────────▼─────────────────────────────┐
│  資料庫層（PostgreSQL）                     │
│  TIMESTAMPTZ / TSTZRANGE                   │
│  永遠 UTC                                   │
└────────────────────────────────────────────┘
```

## 📅 UTC 日期比較

用於統計過濾，避免時區問題：

```dart
// 問題：UTC 12/27 16:00 → 本地 12/28 00:00
// 結果：12/27 的訓練被錯誤算成 12/28！

// ✅ 正確：使用 UTC 日期比較
if (DateTimeUtils.isWithinUtcDateRange(trainingDate, startDate, endDate)) {
  // 在範圍內
}

// ✅ 檢查是否同一天
if (DateTimeUtils.isSameUtcDate(dt1, dt2)) {
  // 同一天
}
```

## 📚 API 參考

| 方法 | 用途 | 返回值 |
|------|------|--------|
| `parseIsoTimestamp()` | 解析 ISO 8601 為本地時間 | `DateTime` |
| `formatToUtcIso()` | 格式化為 UTC ISO | `String` |
| `parsePostgresTimestamp()` | 解析 PostgreSQL 時間戳 | `DateTime` |
| `parseTstzRange()` | 解析 TSTZRANGE | `Map<String, DateTime>` |
| `formatToTstzRange()` | 格式化為 TSTZRANGE | `String` |
| `isWithinUtcDateRange()` | UTC 日期範圍檢查 | `bool` |
| `isSameUtcDate()` | 同一 UTC 日期 | `bool` |

詳見：`@docs/DATETIME_UTILS_GUIDE.md`
